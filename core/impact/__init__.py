"""P12 — impact attribution.

Turns real per-edge SUMO output (edgeData + queue) for the baseline and the
scenario arms into: per-edge deltas, a deterministic impact class, a
direct/secondary/unchanged attribution, and a ranked list of critical
junctions — plus a summary whose every number traces to simulation output.
"""
